
<%= form_for @code_submission do |f| %>
<% if @code_submission.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@code_submission.errors.count, "error") %> prohibited this poll from being saved:</h2>

    <ul>
    <% @code_submission.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>


<h1>Submit your code for review</h1>
  <%= f.label "Enter Code Url" %>
  <%= f.text_field :url %>
  <%= f.label "Code Description" %>
  <%= f.text_area :description, :placeholder => "Enter description...", :size => "100" %><br>
  <%= f.submit "Submit" %>
<% end %>