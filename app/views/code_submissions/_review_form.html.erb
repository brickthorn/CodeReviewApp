<%= form_for @review, :remote => true do |f| %>
<%= f.label "Submit a Review:", :class => "submit-text" %>
<%= f.text_area :body, :rows => "4", :class => "span6" %><br />
<%= f.hidden_field :code_submission_id %>
<%= f.submit "Submit", :class => "btn btn-info"  %>
<% end %>