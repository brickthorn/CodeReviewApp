<%= form_for @code_submission do |f| %>
  <% if @code_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_submission.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% @code_submission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

        <p><%= f.text_field :url, :placeholder => "Enter github gist/repo url..." %></p>
        <p><%= f.text_field :title, :placeholder => "Enter title..." %></p>
        <p><%= f.text_area :description, :placeholder => "Enter description...", :rows => "2" %></p>
        <p><%= f.select :language, code_languages, :prompt => "Select Language" %></p>

      <%= f.submit "Submit", class: "btn btn-success btn-large" %>
<% end %>